{% extends 'base.html.inside.twig' %}
{%  block title %}Form{%  endblock %}
{% block dataTableScript %}
    <script>
        $(document).ready( function () {
            $('#myTable').DataTable( {
                scrollY: 600,
                "info": false,
                scrollCollapse: true,
                "paging": false,
                "oLanguage": {
                    "sSearch": "Filter"
                },
                "columnDefs": [
                    { "orderable": false, "targets": 4 }
                ]
            } );
        } );
    </script>
{%  endblock %}
{% block content %}
    <h1>Form</h1>
    {{ form_start(afdelingForm, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(afdelingForm) }}

    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(afdelingForm) }}
{% endblock %}
