{% extends 'base.html.navbar.twig' %}
{%  block title %}Trainingsfactory - Registreer{%  endblock %}
{% block content %}

    {#{%  if error %}#}
        {#<div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>#}
    {#{% endif %}#}
    <div class="col"></div>
    <div class="col-8">
    <h3>Registreren</h3>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </div>
    <div class="col"></div>
{% endblock %}
